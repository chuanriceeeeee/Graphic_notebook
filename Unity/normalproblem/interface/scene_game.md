- scene界面默认关闭许多渲染设置
### 预览动画的正确姿势：不要一直点 Play！
- **致命陷阱：** Play（运行）模式下修改的任何材质、参数，退出运行后会**全部重置丢失**（白干）。
- **TA 标准工作流：** 在 Scene 窗口的特效菜单中勾选 **`Always Refresh` (始终刷新)**。这样不进 Play 模式，时间 (`_Time`) 也会流逝，可以安全地实时预览水波和噪点动画，且调好的数据会自动保存。